# Tracking Setup

## Tracked branch

The local `feature/remote-sync` branch should track the remote `origin/feature/remote-sync` branch.

## Why it matters

Tracking makes synchronization predictable: the developer clearly knows where updates come from and where new work is published by default.
