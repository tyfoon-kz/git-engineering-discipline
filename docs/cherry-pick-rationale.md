# Cherry-Pick Rationale

## Why only one commit was moved

Only one finished change was needed in the target branch. Moving the entire branch history would have transferred unrelated context.

## Practical point

`cherry-pick` is about selective reuse, not branch migration.
