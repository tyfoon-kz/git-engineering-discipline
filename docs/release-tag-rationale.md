# Release Tag Rationale

The tag `v0.1.0` should mark the point where:
- repository boundaries are cleaned up;
- the project has a stable ignore policy;
- the team can identify a first controlled release point.
