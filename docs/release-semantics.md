# Release Semantics

- `fix` usually maps to `PATCH`
- `feat` usually maps to `MINOR`
- `BREAKING CHANGE` maps to `MAJOR`

## Why it matters

The commit message is not only a note for humans. It also helps define the release meaning of the change.
