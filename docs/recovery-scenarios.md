# Recovery Scenarios

## Working tree mistake

Use `restore` when an unwanted file change should disappear without becoming part of history.

## Local commit mistake

Use `reset` when the commit shape must be corrected before publication.

## Published mistake

Use `revert` when the bad change is already shared and must be undone without rewriting common history.
